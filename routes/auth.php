<?php

use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| Authentication routes — implemented in Phase 1
|--------------------------------------------------------------------------
| Login, logout, registration, password reset, email verification, and
| Google OAuth (Socialite). Loaded with the 'web' middleware group.
|
| Spec: docs/04-features/02-auth-accounts-kyc.md
*/

// Route::get('/login', ...)->name('login');
// Route::post('/login', ...);
// Route::get('/register', ...)->name('register');
// Route::get('/auth/google/redirect', ...)->name('auth.google.redirect');
// Route::get('/auth/google/callback', ...)->name('auth.google.callback');
