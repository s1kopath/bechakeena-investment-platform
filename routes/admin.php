<?php

use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| Admin panel routes (React + Inertia) — implemented in Phase 2
|--------------------------------------------------------------------------
| Loaded with URL prefix "admin" and route-name prefix "admin." (see
| bootstrap/app.php). Will use a separate `admin` auth guard + spatie RBAC.
|
| Spec: docs/04-features/05-admin-panel.md
*/

// Route::middleware('auth:admin')->group(function () {
//     Route::get('/', [Admin\DashboardController::class, 'index'])->name('dashboard');
//     Route::resource('listings', Admin\ListingController::class);
// });
